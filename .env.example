# Local development (SQLite by default)
# Leave DATABASE_URL unset to use local SQLite at data/db/churn_app.db
# DATABASE_URL=sqlite:///data/db/churn_app.db

# Production example (Neon/Supabase Postgres)
# DATABASE_URL=postgresql+psycopg2://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require
